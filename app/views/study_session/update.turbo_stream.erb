<%= turbo_stream.replace "current_lesson_group",  partial: 'lessons/default/lesson_group',
            locals: 
                { lesson_group: @current_lesson_group
                }
        %>

<%= turbo_stream.update "lesson_group_position", html: @current_lesson_group.position %>
<%= turbo_stream.update "flashes", partial: "partials/flash" %>