<%= form_with(model: rich_block) do |form| %>
  <% if rich_block.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rich_block.errors.count, "error") %> prohibited this rich_block from being saved:</h2>

      <ul>
        <% rich_block.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
 <div>
    <%= form.fields_for :lesson_block do |lesson_block_form| %> 
        <%= lesson_block_form.label :title %>
        <%= lesson_block_form.text_field :title %> 
        <%= lesson_block_form.hidden_field :lesson_group_id, value: @lesson_group_id %>
      <% end %>
  </div>

  <div class="hidden">
      <%= form.label :subheader %>
      <%= form.text_field :subheader %> 
  </div>
  
  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.rich_text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
