  <% if @lesson.image.attached? %>
  <div class="container flex justify-center relative">
     <%= image_tag @lesson.image.variant(resize_to_fill: [1280, 120]), class: 'sepia-[.25] ' %>

    <div class="bg-green-200 opacity-20 absolute left-0 top-0 bottom-0 w-full"></div>
     <h1 class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-max">
        <%= lesson.title %> 
      <h1>
  </div>
  <% end %>


<div class="bg-white flex gap-2 items-center justify-center py-12 text-3xl mb-8">
    <div>
        <%= lesson.title %> 
    </div>
    <div>
        <%= lesson.description %> 
    </div>
   </div>

